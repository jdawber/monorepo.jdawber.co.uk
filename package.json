{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "yarn --cwd packages/app dev",
    "build": "yarn --cwd packages/app build",
    "start": "yarn --cwd packages/app start"
  },
  "dependencies": {
    "vercel": "^20.1.2"
  }
}
